---
slug: "secure-cloud-storage"
title: "تخزين سحابي آمن"
problemOneLiner: "ملفات كل المستخدمين في قاعدة واحدة، يفصلها فلتر واحد قد يُنسى."
year: 2026
status: "in-progress"

surface:
  what: "تطبيق ويب لتخزين الملفات متعدّد المستخدمين، كل صف وكل ملف فيه مقيَّد بحساب واحد، والتقييد تفرضه قاعدة البيانات لا كود التطبيق. ويُتاح مسح الوجه بديلًا عن كتابة كلمة المرور."
  why: "التخزين متعدّد المستخدمين يفشل في اتجاه واحد: استعلام ينسى فلترًا فيُعيد بيانات شخص آخر. وضع القاعدة داخل Postgres يعني أن الفحص لا يمكن تخطّيه في أي نقطة نهاية تُضاف لاحقًا، ولا من أي مطوّر يكتبها."
  forWhom: "من يخزّن مستندات شخصية ويحتاج أن يكون حسابه حدًّا مفروضًا، لا عُرفًا يُفترَض أن يتذكّره الكود."

deep:
  layers:
    - id: "browser"
      label: "المتصفح"
      note: "يرفع الملفات، ويحسب بصمة الوجه محليًا عند الدخول بها. ولا تصله أبدًا بيانات غيره ليقارن بها."
    - id: "middleware"
      label: "الوسيط"
      note: "يجدّد كعكة الجلسة ويحوّل الطلبات غير الموثّقة قبل عرض أي صفحة محمية. ويتحقق من التوكن لدى خادم المصادقة بدل الوثوق بالكعكة."
    - id: "app"
      label: "تطبيق Next.js"
      note: "مكوّنات ودوال على الخادم. كل صفحة محمية تعيد فحص الجلسة رغم فحص الوسيط لها — تكرار مقصود لا سهو."
    - id: "auth"
      label: "مصادقة Supabase"
      note: "تصدر الجلسات وتتحقق منها. دخول الوجه يمرّ من هنا بتوكن لمرة واحدة، فلا تنشأ الجلسة إلا بطريق واحد."
    - id: "rls"
      label: "أمان مستوى الصف"
      note: "كل جدول مقيَّد بـauth.uid() داخل Postgres. الصلاحيات تعيش هنا لا في معالجات المسارات."
    - id: "edge"
      label: "دالة الحافة"
      note: "المستدعي الوحيد المسموح له بمقارنة التقاط بحسابات أخرى، والمكان الوحيد الذي يستحق حدّ محاولات."
    - id: "pgvector"
      label: "pgvector"
      note: "يحفظ بصمات من 128 رقمًا بفهرس HNSW على مسافة L2. ولا تُحفظ أي صورة."
    - id: "storage"
      label: "تخزين Supabase"
      note: "دلو خاص للملفات. والتنزيلات تصدر كروابط موقّعة عمرها 60 ثانية."
  architectureNotes: "الطبقات مرتّبة حول قاعدة واحدة: لا يُؤتمَن المتصفح على شيء يخص حسابًا آخر. لهذا تجلس الصلاحيات في Postgres لا في معالجات المسارات، ولهذا فإن العملية الوحيدة التي تحتاج فعلًا أن تقرأ عبر الحسابات — مطابقة التقاط بكل البصمات المسجَّلة — أُخرِجت من التطبيق إلى دالة قاعدة بيانات لا ينفّذها إلا دور الخدمة. ودالة الحافة موجودة لتكون الباب الضيّق الوحيد إليها. وللسياق، هذا مشروع إعادة كتابة: الأصل كان تطبيق سطح مكتب بـPyQt5 على قاعدة MySQL محلية، حيث كان جهاز المستخدم الواحد يوفّر ذلك العزل مجانًا. والانتقال إلى المتصفح يعني إعادة بناء كل تلك الحدود صراحةً."
  decisions:
    - question: "أين يجب أن تعيش الصلاحيات؟"
      optionA: "في معالجات المسارات وكود الصفحات، تُفحَص مع كل طلب."
      optionB: "في Postgres، كأمان مستوى صف على كل جدول."
      chosen: "أمان مستوى الصف."
      because: "الفحص على مستوى التطبيق يجب تكراره عند كل موضع استدعاء جديد، وأول موضع يُنسى هو تسريب بيانات. أما RLS فتفرضه القاعدة نفسها مهما كان العميل السائل أو الاستعلام المكتوب."
    - question: "كيف يُقارَن وجه بكل الحسابات المسجَّلة الأخرى؟"
      optionA: "إرسال البصمات المحفوظة إلى المتصفح والمقارنة هناك."
      optionB: "دالة SECURITY DEFINER تنفيذها مقصور على service_role، يُوصل إليها عبر دالة حافة."
      chosen: "دالة القاعدة خلف دالة حافة."
      because: "أي تصميم يرى فيه المتصفح بصمات الآخرين هو تسريب بيانات بيومترية مهما كان السائل. وإبقاء المقارنة داخل Postgres يعني أن البصمات الخام لا تغادره أصلًا."
    - question: "أي مسافة يستعمل pgvector للمطابقة؟"
      optionA: "مسافة الجيب تمام (cosine)."
      optionB: "المسافة الإقليدية (L2)."
      chosen: "الإقليدية."
      because: "بُني أولًا على cosine ثم غُيِّر. مكتبة face-api.js توثّق عتبتها المرجعية 0.6 بدلالة المسافة الإقليدية، فعتبة cosine تصير رقمًا بلا ما يعايره."
    - question: "كيف يُصدِر دخول الوجه جلسةً؟"
      optionA: "signInWithOtp."
      optionB: "auth.admin.generateLink ثم استبدال التوكن عبر verifyOtp."
      chosen: "generateLink."
      because: "الأول يرسل بريدًا حقيقيًا مع كل محاولة، فينفق حصة البريد على مسار لم يحتج صندوق بريد أصلًا."
  challenges:
    - whatBroke: "صفحة تكتب في قاعدة البيانات وفي التخزين بهوية المستخدم الداخل، كانت تُفتح بلا أي جلسة."
      howDiagnosed: "الوسيط كان يحتفظ بقائمة مسارات عامة ويطابقها كبادئة. و/register في تلك القائمة، فورث كل مسار تحته الاستثناء نفسه — وخطوة ما بعد التسجيل تقع تحته مباشرة. ولم يفشل شيء بصوت عالٍ؛ الصفحة ببساطة كانت تُعرَض لزائر غير مسجَّل."
      fix: "قائمة المسارات العامة قُسِّمت بحسب طريقة المطابقة: /login و/register بتطابق تام، بينما بقي /auth بمطابقة بادئة عن قصد لأن /auth/confirm يُفتح من رابط بريد قبل وجود أي جلسة. وسبب التقسيم مكتوب بجانب القائمة حتى لا يلغيه تعديل لاحق بصمت."
    - whatBroke: "تسجيل الوجه لا يكتمل على كروم. الأوزان تُحمَّل، ثم يموت أول استدعاء لاستخراج البصمة على ملف ثنائي مفقود."
      howDiagnosed: "TF.js يسجّل محرّكَي webgl وwasm بالأولوية نفسها، فعلى جهاز بلا WebGL يقع الاختيار بصمت على wasm — وهو محرّك لا يشحن المشروع ملفاته الثنائية ولا يستدعي setWasmPaths() ليدلّه عليها. وكان الكود أيضًا يبدأ تحميل الأوزان قبل أن يُحسم tf.ready()، فيصطدم بمحرّك مُسجَّل غير مُهيَّأ."
      fix: "دالة selectBackend() تفرض webgl، وتسقط إلى cpu عند فشله، وتنتظر tf.ready() قبل أي تحميل للأوزان. مُتحقَّق منه عمليًا على كروم."
  security: "الصلاحيات تُفرَض في Postgres لا في كود التطبيق: كل جدول يحمل أمان مستوى صف مقيَّدًا بـauth.uid(). والوسيط يتحقق من الجلسة بـgetUser() لا بـgetSession()، أي أن التوكن يُفحَص لدى خادم المصادقة بدل الوثوق به من الكعكة. والعملية الوحيدة التي تعبر بين الحسابات — مطابقة التقاط بكل البصمات المسجَّلة — تجري داخل دالة SECURITY DEFINER نُزع تنفيذها عن anon وauthenticated ومُنح لـservice_role وحده، فتصير دالة الحافة الطريق الوحيد إليها. وهذه النقطة محدودة بعشر محاولات لكل IP كل خمس دقائق، وتُعيد توكنًا لمرة واحدة لا جلسةً. والتسجيل يحفظ بصمة من 128 رقمًا ولا شيء غيرها: نسخة سابقة كانت ترفع صورة JPEG لصورة مصغّرة في البروفايل، وحين حُذفت المصغّرة صار الرفع يحتفظ بصورة بيومترية لا يقرأها شيء، فأُوقف من المصدر. والملفات في دلو خاص لا تخرج إلا كروابط موقّعة عمرها 60 ثانية. هذا تحكّم في الوصول لا تشفير طرفًا لطرف: الخادم يستطيع قراءة الملفات المخزَّنة، والتصميم لا يدّعي غير ذلك."
  whatIdImprove:
    - "لا توجد اختبارات آلية — لا مشغّل ولا ملفات اختبار. كل شيء تُحقِّق منه يدويًا في المتصفح وباستعلامات SQL على المشروع الحي، وهذا بالضبط نوع التغطية التي لا تصمد أمام إعادة هيكلة."
    - "دخول الوجه راحة لا عامل مصادقة ثانٍ: وجه مطابق وحده يكفي للدخول. وجعله مصادقة ثنائية حقيقية يعني اقترانه بكلمة مرور، وأفضّل أن أسمّيه بدقة على أن أدّعي خاصية لا يملكها التصميم."

stack:
  - "TypeScript"
  - "Next.js"
  - "React"
  - "Supabase"
  - "PostgreSQL"
  - "pgvector"
  - "face-api.js"

links:
  repo: "https://github.com/azzam-dev/graduationproj-web"
  live: "https://graduationproj-web.vercel.app"
---
{/* The deep-dive page builds the Layer Reveal diagram and every section
    directly from the frontmatter above — this body is intentionally empty. */}
