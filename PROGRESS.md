# Progress

آخر تحديث: 2026-09-10. حالة حيّة — حدّثها كلما تغيّر شيء فعلي في الكود أو النشر.
مكتوبة لقارئ لا يملك أي سياق عن المحادثات التي أنتجتها.

**المشروع:** ملف شخصي ثنائي اللغة (En بلا بادئة / Ar تحت `/ar`) على Astro، يعرض
المشاريع بنمط "Surface / Under the Hood": صفحة سطح مختصرة لكل مشروع، وصفحة
`/deep` تفصيلية لمن يريد التفاصيل التقنية.

**المستودع:** `azzam-dev/portfolio` — الفرع الإنتاجي `master`، وكل push ينشر
تلقائيًا على Vercel. أي أن الـpush إطلاق للعامة.

**الرابط العام:** https://portfolio-iota-ten-2fmes3t5ce.vercel.app — مفتوح بلا
تسجيل دخول ويتبع كل push (مُتحقَّق مع `d6dc54f`). **لا تستعمل روابط
`*-me-5abe.vercel.app`** للتحقق من الموقع العام: كلها (ومنها `target_url` الذي
يسجّله GitHub لكل نشر) ترد `302 → vercel.com/sso-api`.

---

## تم

### البنية والنشر
- Astro 5.18.2 + Tailwind 4.3.3 + TypeScript strict + جزيرتا React.
- ثنائي اللغة مع RTL كامل، ووضع فاتح/داكن (توكنز CSS + زر تبديل + احترام
  `prefers-color-scheme` + حفظ الاختيار).
- النشر يعمل. آخر كوميت منشور:
  `gh api repos/azzam-dev/portfolio/deployments --jq '.[0].sha[0:7]'`. تحقّق
  عام: `/` و`/ar/` وصفحة دراسة الحالة وملف السيرة (`application/pdf`) ترد 200،
  و`/about` يرد 404 (مقصود — انظر القرارات).

### الموقع صفحة واحدة
- الأعمال والخبرة والنبذة والتواصل **أقسام** داخل `index.astro` و`ar/index.astro`
  (`#top`, `#work`, `#experience`, `#about`, `#contact`؛ الترقيم `01`–`04`)،
  وروابط الشريط مراسٍ إليها. المسارات الستة المبنية: `/`، `/ar/`، و`/work/[slug]`
  و`/work/[slug]/deep` بنسختيهما.
- **شاشة كاملة لكل قسم:** `min-h-svh` بمحتوى موسَّط، مع
  `scroll-snap-type: y mandatory` و`snap-start snap-always`.
- **كل قسم يتسع في شاشة جوال واحدة — عدا "الخبرة".** قيس على 375×701 و360×740
  و390×844 و430×932 و768×1024 و1280×800 باللغتين: لا قسم آخر يفيض. على الجوال
  (تحت 640px) تختفي الفقرة الثانية في "نبذة"، ومسافات "الأعمال" أضيق (تحت 768px).
  أضيق هامش: "الأعمال" بالإنجليزية على 375×701 = **6px**. "الخبرة" تطول على
  الجوال (1004px على 375×701) ويُمرَّر فيها — انظر القرارات.
- الشريط العلوي `sticky` بخلفية معتمة. من `md`: خمسة روابط مركزية (أقل مسافة بينها
  وبين الأزرار 67px على 768). تحته: زر "القائمة" (`<details class="site-menu">`)
  تنسدل منه الروابط بعرض الشاشة، ويُغلق عند اختيار رابط أو نقرة خارجه أو Escape.
- **قفزات الشريط لا ترتدّ على الجوال** — مجرّب على جوال حقيقي (2026-09-10).
  الآلية في "قيد العمل" والسبب في "القرارات".
- **تبديل اللغة يحفظ القسم:** من "تواصل" بالإنجليزية يصل إلى "تواصل" بالعربية
  والعكس — مجرّب على جوال حقيقي.

### قسم "الخبرة" وشهادة التدريب
- تدريب تعاوني في سدايا — المركز الوطني للذكاء الاصطناعي، إدارة هندسة الجودة،
  24 أغسطس – 19 أكتوبر 2025. أربع نقاط مستخلصة من التقارير المرحلية الثلاثة
  (Dataiku DSS: ماكرو، Checks/Metrics/Scenarios؛ اختبار الواجهة الخلفية؛ التحقق من
  البيانات)، ووسوم، في ثابت `coop` أعلى كل صفحة رئيسية. جملة سدايا حُذفت من "نبذة".
- **بطاقة الشهادة** `TiltedCard.astro`: تميل نحو المؤشر (7°) وتكبر ×1.08 عند
  المرور. رمادية، وعند المرور تظهر ألوانها الحقيقية في بقعة تلحق المؤشر (طريقة
  ChromaGrid) ويصير إطارها بلون `--accent`؛ التركيز بلوحة المفاتيح يظهرها كاملة.
  الضغط يفتح الشهادة كاملة بألوانها في `<dialog>` (زر إغلاق، ونقرة الخلفية،
  وEscape). على الجوال: رمادية، والضغط يفتحها.
- الصورة `src/assets/sdaia-coop-certificate.png` (2526×1786، مرسومة من PDF الشهادة
  بـpdf.js)؛ `astro:assets` يولّد منها webp: 7–21KB للبطاقة و100KB للعارض.
- مُتحقَّق محليًا: الميلان والتكبير والألوان عند مرور مؤشر حقيقي، العارض (345×244
  على جوال 375)، القائمة، ونقاط الالتقاط داخل "الخبرة". **لم يُجرَّب بعد على جوال
  حقيقي.**

### خلفية Dot Lens
- شيدر WebGL (كود خارجي من Originkit) يغطي **الصفحة الرئيسية وحدها**:
  `DotLens.tsx` + غلاف الثيم `DotLensBackground.tsx`.
- مثبّت على إطار العرض (`fixed inset-0`, `z-index:-1`)، فالكانفس بحجم شاشة واحدة
  مهما طالت الصفحة.
- يتتبّع المؤشر (مُتحقَّق بأحداث مؤشر حقيقية)، وينسحب عن الروابط والأزرار.

### النظام البصري
- كل توكنز النص تعبر AAA (7:1) في الوضعين؛ الأرقام مكتوبة كتعليقات بجانب كل
  قيمة في `global.css`.
- مقياس خط سائل بـ`clamp()`: `.t-display` `.t-h2` `.t-lead` `.t-body`.
- Space Grotesk للعناوين، Inter للمتن، JetBrains Mono للأكواد والبريد، IBM Plex
  Sans Arabic للعربية عبر `:lang(ar)`.
- قوالب مشتركة في `@layer components`: `.eyebrow` `.chip` `.btn-accent`
  `.btn-ghost` `.link-row`، تستعملها الرئيسية وصفحات دراسة الحالة.
- الأزرار: `.btn-accent` (ممتلئ: ظل من طبقتين + لمعة داخلية + `scale(0.98)`
  عند الضغط) و`.btn-ghost` (إطار، لروابط Repo/Live/Demo في دراسة الحالة). عند
  المرور يغمق الممتلئ في الفاتح ويفتح في الداكن، ويأخذ الـghost حدًّا بلون
  الـaccent وخلفية خفيفة منه.
- حلقة تركيز `:focus-visible` للوحة المفاتيح.

### المحتوى
- دراسة الحالة الوحيدة (`secure-cloud-storage`) مكتوبة من المشروع الحقيقي
  `azzam-dev/graduationproj-web` (Next.js + Supabase بصلاحيات مفروضة في قاعدة
  البيانات)، و`links.repo`/`links.live` يردّان 200.
- قسم "نبذة": "خرّيج علوم حاسب من جامعة المجمعة" + فقرة الاهتمام بحدود الأنظمة
  (من `sm`)؛ الرقاقات من مهارات السيرة. التدريب انتقل إلى "الخبرة".
- زر تحميل السيرة في الـhero باللغتين → `public/Azzam-Alfahad-CV.pdf`.
- لا نص مؤقت (`Replace with…`) في `src/`.

### أعطال أُصلحت (للمرجع — كلها رُصدت بالقياس لا بالنظر)
- **CSS خارج `@layer` يتغلّب على Tailwind:** `a { color: inherit }` ألغت ألوان
  الروابط، و`* { border-color }` ألغت حدّ `.btn-ghost:hover`. كلتاهما في
  `@layer base` الآن.
- **زر "الرئيسية" كان يعيد تحميل الصفحة** — كان الرابط الوحيد بلا hash. صار `/#top`.
- **روابط الشريط كانت ترتدّ على الجوال:** من "الأعمال"، "الرئيسية" تذهب ثم ترجع.
- **"نبذة" و"الأعمال" كانتا أطول من شاشة الجوال:** 900px و755px على 375×701.
- **تبديل اللغة كان يهبط في أعلى الصفحة الأخرى** مهما كان القسم.
- **تتبّع المؤشر لم يكن يعمل:** عنصر عند `z-index:-1` لا يصله حدث مؤشر؛ الاستماع
  نُقل إلى `window`.
- **تقطّع الحروف العربية** حيثما لمسها `uppercase`/`tracking-widest`/`.mono`.

---

## قيد العمل

**لا شيء مفتوح في الكود.** قسم "الخبرة" مدفوع إلى `master` (2026-09-10)
ومُتحقَّق محليًا، **وينتظر تجربة صاحب المشروع على جواله:** القائمة ثم "الخبرة"،
والتمرير داخلها حتى نهايتها، والضغط على الشهادة. الملفات: `src/components/TiltedCard.astro`
(جديد، والسكربت فيه: `reset()` ومستمعا `pointermove`/`pointerleave` ثم `click`
الذي يستدعي `viewer.showModal()`)، `src/components/Header.astro` (مصفوفة `links`،
`<details class="site-menu">`، وإغلاقه في السكربت)، `src/pages/index.astro`
و`src/pages/ar/index.astro` (قسم `#experience` وثابت `coop`)، `src/i18n/ui.ts`
(مفاتيح `nav.experience` `nav.menu` `experience.*`)، `src/styles/global.css`
(`--chroma-filter` و`@property --chroma-x/--chroma-y` و`.tilt-card*`)،
`src/assets/sdaia-coop-certificate.png`.

آخر ما لُمس قبله، مكتمل ومجرّب على جوال حقيقي:
- `src/components/Header.astro` — السكربت أسفل الملف (IIFE):
  - مستمع `click` على روابط الشريط: لرابط إلى الصفحة نفسها يمنع السلوك الافتراضي،
    يستدعي `holdSnap()` ثم `target.scrollIntoView()` (ناعم عبر CSS) و`pushState`.
  - `holdSnap()`: `scroll-snap-type: none` على `<html>` + مستمع `scroll` +
    `waitForQuiet(400)`.
  - `onScroll()` → `waitForQuiet(150)`؛ و`waitForQuiet(ms)` يؤجّل `release()`
    الذي يعيد الالتقاط ويزيل المستمع.
  - `landOnHash()`: عند التحميل، إن كان في الرابط hash يشير إلى `<section>`،
    يقفز إليه فورًا (`behavior: "instant"`) تحت `holdSnap()`.
- `src/components/LanguageToggle.astro` — `carrySection()` على `click`
  و`auxclick`: يلحق بالـhref معرّف القسم الذي يقطع منتصف الشاشة.

---

## التالي

### 1. اختبارات e2e بـPlaywright تحرس ما أُصلح اليوم
**لماذا:** لا يوجد أي اختبار آلي، وأهم ما أُصلح هشّ: هامش "الأعمال" 6px فقط،
وسكربتا القفز وتبديل اللغة يعتمدان على تطابق معرّفات الأقسام بين اللغتين. والمتصفح
المدمَّج المستخدَم في الفحص لا يشغّل `requestAnimationFrame` أصلًا، أما Chromium
الذي يشغّله Playwright فيشغّله، فيصير التمرير الناعم قابلًا للاختبار.

**التنفيذ:**
1. `npm i -D @playwright/test` ثم `npx playwright install chromium`.
2. `playwright.config.ts` في الجذر: `testDir: "tests/e2e"`، مشروع `chromium` فقط،
   `use.baseURL: "http://localhost:4322"`، و
   `webServer: { command: "npm run build && npm run preview -- --port 4322", url: "http://localhost:4322", reuseExistingServer: true, timeout: 180_000 }`
   (المنفذ 4322 حتى لا يصطدم بخادم التطوير على 4321).
3. في `package.json`: `"test:e2e": "playwright test"`. **لا تضفه إلى `build`** —
   Vercel يشغّل `build` ولا يملك متصفحات.
4. في `.gitignore`: `test-results/` و`playwright-report/`.
5. خمسة ملفات في `tests/e2e/`:
   - `sections-fit.spec.ts`: لكل مقاس من `375×701` `360×740` `390×844` `430×932`
     `768×1024` `1280×800` ولكل من `/` و`/ar/`: كل `main section[id]` **عدا
     `#experience`** ارتفاعه (`offsetHeight`) ≤ ارتفاع النافذة. ولـ`#experience`
     على 375×701: `scrollTo` إلى (قمته + ارتفاعه − ارتفاع النافذة + 60) ثم تحقّق
     أن `scrollY` استقر على (قمته + ارتفاعه − ارتفاع النافذة) — أي أن نقطة
     الالتقاط في أسفله تعمل.
   - `nav.spec.ts` (مقاس 375×812): اقفز فورًا إلى `#work` (اضبط
     `document.documentElement.style.scrollBehavior = "auto"` مؤقتًا)، افتح
     `header .site-menu summary` ثم اضغط `.site-menu a[href="/#top"]` (تحت `md`
     الروابط في القائمة)، تحقّق أن `.site-menu` أُغلقت، وانتظر بـ`expect.poll` حتى يصير
     `document.documentElement.style.scrollSnapType` مساويًا `""`، ثم تحقّق أن
     `scrollY` يساوي `el.getBoundingClientRect().top + scrollY` لـ`#top` بفارق ≤1px.
     كرّر لـ`#experience` و`#about` و`#contact`. وعلى 1280×800: الروابط في
     `header > div > nav` مباشرة و`.site-menu` مخفية.
   - `certificate.spec.ts` (1280×800): اضغط `.tilt-card__face` ← `dialog` مفتوح
     وعرض صورته > 600px ← Escape يغلقه. وحرّك المؤشر إلى نقطة في البطاقة ←
     `getComputedStyle(.tilt-card__color).opacity` يصير `1`، و`maskImage` بعد
     انتهاء الانتقال يحوي `at <x>px <y>px` قريبًا من موضع المؤشر داخل البطاقة.
   - `lang-toggle.spec.ts`: من `/` اقفز فورًا إلى `#contact`، اضغط `#lang-toggle`،
     `waitForURL(/\/ar\/?#contact$/)`، انتظر عودة الالتقاط كما سبق، وتحقّق أن
     `scrollY` على قمة `#contact` (≤1px). والعكس: من `/ar/` عند `#about` إلى
     `/#about`. وعلى `/work/secure-cloud-storage` بعد تمرير 400px: `href` الزر
     بعد `auxclick` مصطنع لا يحوي `#`.
   - `mirror.spec.ts`: قائمة معرّفات `main section[id]` في `/` تساوي نظيرتها في
     `/ar/` وبالترتيب نفسه.
6. **أثبت أن الاختبارات تمسك الخلل:** احذف `hidden sm:block` مؤقتًا من الفقرة
   الثانية في "نبذة" (`src/pages/index.astro`) ← لازم يفشل `sections-fit` على
   375×701 ← أرجعه.

**معيار الانتهاء:** `npm run test:e2e` كله أخضر، و`npm run build` بلا أخطاء.
(`tsconfig.json` يشمل `src/**` فقط، فـ`astro check` لا يفحص `tests/` ولا
`playwright.config.ts` — Playwright يترجم TypeScript بنفسه، فلا تعدّل `include`.) ثم:
أضف `npm run test:e2e` إلى قسم الأوامر في `CLAUDE.md` واحذف سطر "لا يوجد test
suite"، واحذف البند 2 من "مشاكل معروفة" هنا. **لا تعمل commit ولا push قبل موافقة
صاحب المشروع** — طريقته: يرى النتيجة أولًا ثم يأذن بالرفع.

### 2. مشروع ثانٍ (يحتاج محتوى من صاحب المشروع)
ملفّا MDX في `src/content/projects/{en,ar}/` بنفس الـschema. البنية جاهزة:
`FeaturedProject` يعرض الأول و`ProjectCard` البقية تلقائيًا. قِس بعدها بـ
`sections-fit` — بطاقة ثانية قد تُفيض "الأعمال" على الجوال.

---

## قرارات

كل قرار مع البديل المرفوض والسبب.

### الخبرة والشهادة (2026-09-10)
- **تأثير TiltedCard مبني من جديد في Astro، لا مكوّن React Bits نفسه** (قرار صاحب
  المشروع). المكوّن الأصلي يحتاج مكتبة `motion` وجزيرة React ثالثة؛ التأثير نفسه
  (ميلان نحو المؤشر + تكبير) يكفيه سكربت صغير يكتب `--rx`/`--ry`/`--s` وانتقال CSS.
  الميلان على طبقة داخلية، والقياس على الزر غير المائل، حتى لا يدور المستطيل
  المقاس تحت المؤشر.
- **التكبير 1.08 لا 1.45 كما في مثال React Bits** (قرار صاحب المشروع): 1.45
  تغطي النص المجاور. **رُفض** أيضًا: بلا تكبير.
- **الميلان فقط مع `(hover: hover) and (pointer: fine)` وبلا `prefers-reduced-motion`.**
- **الألوان بطريقة ChromaGrid** (React Bits): الصورة رمادية (وأعتم ×0.78 في
  الداكن)، وبقعة ألوان حقيقية نصف قطرها 300px تلحق المؤشر بتأخير 0.45s على منحنى
  `power3.out`، وتنطفئ في 0.6s عند الخروج — قيم مثال صاحب المشروع. **رُفض قبلها:**
  تلوين الصورة بلون الموقع (رمادي + طبقة `--accent` بـ`mix-blend-mode: color`) —
  جُرّب ولم يعجب صاحب المشروع. **وطُبّقت الألوان وحدها** على البطاقة المائلة الحالية
  (قرار صاحب المشروع)؛ رُفضت بطاقة ChromaGrid كاملة (خلفية متدرّجة ونص داخلها، بلا
  ميلان).
- **البقعة نسخة ملوّنة مقنّعة فوق الرمادية، لا `backdrop-filter` كما في ChromaGrid:**
  طبقة `backdrop-filter` داخل عنصر مائل ثلاثي الأبعاد مخاطرة في الأداء والدعم،
  والنسختان بنفس `srcset` و`sizes` فالمتصفح يجلب الملف مرة واحدة. والتأخير بـ
  `@property` + `transition` بدل GSAP (بلا مكتبة ولا حلقة `requestAnimationFrame`).
- **الجوال: رمادية، والضغط يفتح العارض بألوانها** (قرار صاحب المشروع). **رُفض:**
  ألوان حقيقية دائمًا على الجوال. وقاعدة الإظهار داخل `@media (hover: hover)` لأن
  `:hover` يعلق على اللمس.
- **العارض `<dialog>` أصلي لا مكتبة lightbox ولا فتح الصورة في تبويب:** Escape
  والتركيز والخلفية تأتي مع العنصر؛ عرضه صريح (`min(92vw, 1100px)`) لأن الـdialog
  يتقلّص لمحتواه فانهارت الصورة إلى 86×61.
- **قائمة الجوال `<details>`، لا صف مضغوط ولا إخفاء "Home"** (قرار صاحب المشروع):
  خمسة روابط فاضت 375px بـ59px، وحتى بلا "Home" فاضت 360px بـ7px. **رُفض** أيضًا:
  قسم بلا رابط في الشريط.
- **"الخبرة" تطول على الجوال بكل محتواها** (قرار صاحب المشروع: "خلني أمرر"). كانت
  900px على 375×701، والنقاط الأربع وحدها 377px. **رُفض:** نقطتان + شهادة مصغّرة
  على الجوال (يتسع، لكن يُخفي نصف الخبرة)، واختصار الصياغة (لا يكفي وحده)، وإطفاء
  الالتقاط على الجوال كله. **الآلية:** `<span>` في أسفل القسم بـ`snap-end
  snap-always` نقطة التقاط ثانية؛ بدونها كان الالتقاط يقفز من أعلى القسم إلى
  "نبذة". مُتحقَّق: كل موضع داخل القسم يثبت، وما بعد نهايته يعود إلى النهاية.
- **الشهادة صورة لا ملف PDF:** البطاقة والعارض يحتاجان صورة؛ رُسمت من الـPDF بـ
  pdf.js في المتصفح (`intent: "print"` لأن المتصفح المدمَّج لا يشغّل
  `requestAnimationFrame`) — `@napi-rs/canvas` في Node انهار (segfault) على ويندوز.
- **التقارير المرحلية لا تُنشر**، والمحتوى منها نصًا: فيها جوال وبريد المشرف.

### التنقّل والجوال (2026-09-10)
- **الارتداد: الالتقاط يُطفأ أثناء القفزة ويعود بعد سكون التمرير.** السبب: قفزة
  ناعمة تحت `mandatory` تترك القسم القديم هدفًا للالتقاط، وانطواء شريط متصفح
  الجوال منتصف القفزة يغيّر حجم الشاشة فيعيد الالتقاط إليه. عند الإعادة يكون
  أقرب نقطة التقاط هو القسم الهدف نفسه فلا شيء يتحرّك. **رُفض:** `proximity`
  (يُسقط "شاشة = قسم")، وحذف `scroll-behavior: smooth` (يُسقط التمرير الناعم
  كله)، وإطفاء الالتقاط على الجوال (يُسقط التصميم على الجهاز الذي ظهر فيه الخلل).
- **مؤقّت سكون لا حدث `scrollend`:** 150ms بعد آخر حدث `scroll`، و400ms قبل أول
  حدث حتى لا يُعاد الالتقاط قبل أن تبدأ القفزة إن تأخر أول إطار (الحقل شيدر
  ثقيل). رُفض `scrollend` وحده: إن كان القسم في مكانه أصلًا فلا تمرير ولا
  `scrollend`، والالتقاط يبقى مطفأً — المؤقّت لازم في كل الأحوال، فصار هو الآلية.
- **السكربت في `Header.astro` لا في صفحتَي الرئيسية:** نسخة واحدة للغتين، ويتجاهل
  أي رابط لصفحة أخرى بمقارنة `pathname`، فيبقى سلوك صفحات دراسة الحالة عاديًا.
- **"نبذة" على الجوال: إخفاء الفقرة الثانية تحت 640px** (قرار صاحب المشروع).
  المطلوب كان ~200px، وتضييق المسافات والخط لا يعطي إلا 40–60px. **رُفض:**
  اختصار النص للجميع (يغيّر نسخة سطح المكتب)، وإطفاء الالتقاط على الجوال.
- **"الأعمال" على الجوال: مسافات أضيق لا محتوى أقل.** `pb-6` للأقسام و`mb-5`
  للعناوين وهوامش أصغر في `FeaturedProject`، و`md:` يعيد قيم سطح المكتب. `pt-20`
  بقي كما هو لأن الشريط اللاصق يغطي أعلى 65px من كل قسم.
- **تبديل اللغة يقرأ القسم من التخطيط لا من `location.hash`:** الـhash يبقى قديمًا
  لمن يمرّر بالعجلة بدل روابط الشريط. القسم يُحمل في الـhash (لا `sessionStorage`)
  فيبقى الرابط قابلًا للمشاركة.
- **الوصول بـhash يقفز فورًا (`landOnHash`) بدل ترك قفزة المتصفح:** قفزة التحميل
  ناعمة هي الأخرى وتبدأ من القسم الأول، فتتعرّض للارتداد نفسه. تغطي أيضًا روابط
  الشريط المتّبعة من صفحات دراسة الحالة.
- **الشريط الرمادي أسفل الصفحة لم يُمسّ له كود:** ظهر في كروم صاحب المشروع وحده؛
  على الرابط العام لا عنصر هناك، وبريف (المحرّك نفسه) يعرض الصفحة سليمة. تعديل
  `DotLens.tsx` بلا إعادة إنتاج كان تخمينًا.

### الصفحة الواحدة والتخطيط
- **حذف الصفحات المفصولة، لا إبقاؤها كتحويلات.** طلب صريح من صاحب المشروع.
  الأثر المقبول: `/about` و`/contact` و`/work` ترد 404.
- **صفحتا دراسة الحالة لم تُدمجا** — يُوصل إليهما من البطاقة لا من الشريط، وهما
  جوهر نمط "Surface / Under the Hood".
- **`min-h-svh` لا `h-svh`، و`svh` لا `vh`.** الأولى لا تقص محتوى فاض؛ والثانية
  تتجنّب أن يدفع شريط متصفح الجوال أسفل القسم خارج المتناول.
- **`mandatory` لا `proximity`، مع مهرب:** `@media (max-height: 700px)` يطفئ
  الالتقاط على الشاشات القصيرة، و`prefers-reduced-motion` يطفئه أيضًا.
- **قاعدة الالتقاط في `<style is:global>` داخل صفحتَي الرئيسية لا في
  `global.css`:** Astro يشحن أنماط الصفحة معها وحدها، فصفحات دراسة الحالة تبقى
  بتمرير عادي.
- **الشريط `sticky`:** بشاشة كاملة لكل قسم يختفي التنقّل بعد أول تمرير لو كان
  في التدفّق. وخلفيته المعتمة ضرورية لأن الحقل مثبّت ويمتد خلفه.
- **التوسيط بـ`left-1/2` الفيزيائية لا `start-1/2`:** المنطقية تقلبه بعيدًا عن
  المركز في RTL.

### خلفية Dot Lens
- **جزيرة React ثانية** خلافًا لاتفاقية "الجزيرة الوحيدة". رُفض CSS خالص: عدسة
  تكبّر النقاط حول المؤشر تحتاج حسابًا لكل بكسل.
- **الكود الخارجي بتعديلات محدودة لا إعادة كتابة:** قوس زائد كان يُفشل
  `astro check`، حذف `"use client"`، الحلقة الموقوفة، المسح الأولي، انسحاب العدسة.
- **الثيم يُحلّ في غلاف منفصل:** `parseColor` يفهم hex/rgb فقط. الغلاف يراقب
  `data-theme` (`MutationObserver`) **و**`matchMedia` معًا.
- **`gl.clear()` بلون الخلفية فور إنشاء السياق:** كانفس `alpha:false` يبدأ أسود.
- **`IntersectionObserver` يوقف الحلقة خارج الشاشة.**
- **أحداث المؤشر من `window`:** المحاولة الأولى (`pointer-events-none` على النص)
  لم تنجح لأن `<main>` يفوز بفحص الإصابة.
- **`fixed` لا `absolute`:** المطلقة تمدّ الكانفس إلى ارتفاع المستند كاملًا.
- **توكن `--field-dot` منفصل عن `--text-muted`:** وإلا زاد كل تحسين لتباين النص
  ضجيج الخلفية.

### النظام البصري
- **الأسهم محارف نصية لا SVG:** `→` ينقلب تلقائيًا في RTL (`Bidi_Mirrored`).
- **تعبئة مسطّحة + لمعة داخلية، لا تدرّج:** التدرّج يخاطر بمظهر قديم.
- **لون المرور يغمق فاتحًا ويفتح داكنًا:** الاتجاهان يرفعان التباين.
- **`.btn-ghost` مستوى ثالث** لروابط Repo/Live/Demo: كانت نصوصًا مسطّرة بجوار زر
  ممتلئ، أي قفزة من لا تأكيد إلى أقصى تأكيد.

### المحتوى
- **دراسة الحالة بتأطير هندسي لا بقصة الترحيل:** تقود بالصلاحيات المفروضة في
  قاعدة البيانات؛ خلفية PyQt5 في `architectureNotes` كسياق.
- **الـ`slug` بقي `secure-cloud-storage`** رغم تغيّر العنوان، حتى لا تنكسر الروابط.
- **رقم الجوال ليس في قسم التواصل، لكنه داخل ملف السيرة المنشور** — كلاهما بقرار
  صريح من صاحب المشروع. **لا تضف الرقم إلى قسم التواصل** بلا طلب جديد.

### تأسيسية (سارية)
- `generateId` مخصص في `src/content/config.ts`: تصادم `slug` بين en/ar كان يُلغي
  إحدى النسختين بصمت.
- صفحات en/ar ملفات منفصلة متطابقة البنية، لا `[locale]` ديناميكي.
- `/deep` صفحة مستقلة قابلة للمشاركة والفهرسة، لا حالة UI مخفية.
- **التحقق قبل أي push بـ`npm run build`** لا `astro build` وحده — الأخير لا
  يشغّل `astro check`، وهذا ما مرّر خطأ type محليًا وأفشل نشرًا سابقًا.
- الدومين المخصص مؤجل — الموقع على دومين Vercel الافتراضي (الرابط العام أعلاه).

---

## مشاكل معروفة

1. **شريط رمادي بنص متراكب أسفل الصفحة — في كروم صاحب المشروع وحده.** ليس من
   الموقع (انظر القرارات). المرجّح شريط أدوات Vercel لمن سجّل دخوله، أو إضافة، أو
   تكبير محفوظ للموقع. **لا تغيّر `DotLens.tsx` بسببه** ما لم يظهر في نافذة كروم
   متخفية.
2. **لا اختبارات آلية** — كل تحقق يدوي. الحل في "التالي" البند 1.
3. **طبقة محتوى Astro تخزّن مؤقتًا:** تعديل `.mdx` قد لا يظهر في `npm run dev`.
   الحل: `rm -rf .astro` وإعادة تشغيل الخادم.
4. **المتصفح المدمَّج المستخدَم في الفحص لا يشغّل `requestAnimationFrame`**
   (0 إطار/ثانية): لا يرسم حقل النقاط ولا يحرّك التمرير الناعم، ولقطات الرئيسية
   بمحاكاة الجوال تخرج فارغة. تحقّق هناك بالقياس فقط، والحركة على جوال أو متصفح
   حقيقي (أو Playwright بعد البند 1). **لم يُشاهَد بعد:** الانجراف التلقائي للحقل.
