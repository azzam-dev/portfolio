# Progress

آخر تحديث: 2026-09-10. حالة حيّة — حدّثها كلما تغيّر شيء فعلي في الكود أو النشر.
اكتبها افتراضًا أن القارئ لا يملك أي سياق عن المحادثات التي أنتجتها.

**المشروع:** ملف شخصي ثنائي اللغة (En بلا بادئة / Ar تحت `/ar`) على Astro، يعرض
المشاريع بنمط "Surface / Under the Hood": صفحة سطح مختصرة لكل مشروع، وصفحة
`/deep` تفصيلية لمن يريد التفاصيل التقنية.

**المستودع:** `azzam-dev/portfolio` — الفرع الإنتاجي `master`، نشر تلقائي على
Vercel عند كل push عبر GitHub App.

---

## تم

### البنية والنشر
- سقالة كاملة: Astro 5.18.2 + Tailwind 4.3.3 + TypeScript strict + جزيرتا React.
- ثنائي اللغة مع RTL كامل، ووضع فاتح/داكن (توكنز CSS + زر تبديل + احترام
  `prefers-color-scheme` + حفظ الاختيار).
- **الموقع منشور على Vercel** — آخر push بتاريخ 2026-09-10 (الأزرار + إصلاح
  الارتداد + مقاسات الجوال في commit واحد). آخر كوميت منشور:
  `gh api repos/azzam-dev/portfolio/deployments --jq '.[0].sha[0:7]'`.
- **الرابط العام:** `https://portfolio-iota-ten-2fmes3t5ce.vercel.app` — مُتحقَّق
  2026-09-10 بلا تسجيل دخول: `/` و`/ar/` وصفحة دراسة الحالة ترد 200، ملف السيرة
  يرد 200 بـ`application/pdf`، و`/about` يرد 404 (مقصود). ويخدم محتوى `a9950c7`.
  **انتبه:** روابط `*-me-5abe.vercel.app` (ومنها `portfolio-me-5abe.vercel.app`
  و`target_url` في سجل النشر على GitHub) **ما زالت** ترد `302 → sso-api`. لم
  يُتحقَّق بعد أن الرابط العام يتبع كل push تلقائيًا — تأكّد منه بعد الـpush القادم.

### الموقع صفحة واحدة
- الأعمال والنبذة والتواصل **أقسام** داخل `index.astro` (`#top`, `#work`,
  `#about`, `#contact`)، وروابط الشريط مراسٍ إليها. **حُذفت ست صفحات**
  (`about`, `contact`, `work/index` بنسختيها) ومعها مفتاح `work.heading`.
  المسارات الستة المبنية الآن: `/`، `/ar/`، و`/work/[slug]` و`/work/[slug]/deep`
  بنسختيهما.
- **شاشة كاملة لكل قسم:** كل قسم `min-h-svh` بمحتوى موسَّط، مع
  `scroll-snap-type: y mandatory` و`snap-start snap-always`. قيس: الأقسام
  الأربعة تساوي ارتفاع الشاشة بالضبط (800px على شاشة 800، و812 على 812)، بلا
  فيض رأسي ولا أفقي.
- الشريط العلوي `sticky` بخلفية معتمة، وروابطه الأربعة مركزية تمامًا من `md`
  فما فوق (انحراف 0px عن مركز الحاوية، مقيس).

### خلفية Dot Lens
- شيدر WebGL (كود خارجي من Originkit) يغطي **الصفحة الرئيسية وحدها** — تُحقّق
  منه بجلب كل الصفحات وفحص ناتجها. `DotLens.tsx` + غلاف `DotLensBackground.tsx`.
- مثبّت على إطار العرض (`fixed inset-0`, `z-index:-1`)، فالكانفس يبقى بحجم شاشة
  واحدة (1265×720) مهما طالت الصفحة.
- **يتتبّع المؤشر، مُتحقَّق منه بأحداث مؤشر حقيقية:** العدسة لحقت المؤشر إلى
  (435,270) مقابل مؤشر عند (435,268).
- **ينسحب عن العناصر التفاعلية:** مؤشر على رابط ⇒ العدسة تبقى بعيدة (قيس: 510
  مقابل مؤشر عند 312). و`.link-row` بخلفية معتمة كطبقة ثانية.

### النظام البصري
- **التباين:** كل توكنز النص تعبر AAA (7:1) في الوضعين. `--text-muted` كان
  **4.90** (بالكاد AA، وهو لون معظم النصوص) وصار **7.17** فاتح و**8.58** داكن.
  `--accent` **7.32** و**8.08**. الأرقام مكتوبة كتعليقات بجانب كل قيمة في
  `global.css`.
- **مقياس خط سائل** بـ`clamp()` في `@layer components`: `.t-display` `.t-h2`
  `.t-lead` `.t-body`. قيس على 1265px: 84/40/18.4px، وعلى 375px: 44/28/16px.
- **خط عرض Space Grotesk** للعناوين، Inter للمتن، JetBrains Mono للأكواد
  والبريد. العربية تتجاوز خط العرض والـtracking السالب عبر `:lang(ar)`.
- **قوالب مشتركة** في `@layer components`: `.eyebrow` (سطر `01 / Work` قبل كل
  عنوان)، `.chip`، `.btn-accent`، `.btn-ghost`، `.link-row`. صفحات دراسة الحالة
  تستعملها كلها بعد أن كانت على أنماط قديمة خاصة بها.
- **الأزرار:** ظل من طبقتين + لمعة داخلية + `scale(0.98)` عند الضغط +
  `min-height: 3rem`. ولون يتغيّر عند المرور: **يغمق** فاتح و**يفتح** داكن —
  الاتجاهان يرفعان تباين النص (7.65→10.13 و8.08→10.22، مقيس).
- **حلقة تركيز للوحة المفاتيح** أُضيفت — لم يكن في المشروع أي `:focus-visible`.

### المحتوى
- **دراسة الحالة الوحيدة مكتوبة من الواقع.** كانت تصف مشروعًا غير موجود (تشفير
  من جهة العميل عبر Web Crypto وتخزين AWS S3)، و`links` فارغة فلا سبيل للتحقق.
  المشروع الحقيقي `azzam-dev/graduationproj-web` هو Next.js + Supabase بصلاحيات
  مفروضة في قاعدة البيانات. كل حقل الآن من مصدر مقروء، و`links.repo` و
  `links.live` مضبوطان (مُتحقَّق: يردّان 200).
- **قسم "نبذة" من السيرة الذاتية:** خرّيج علوم حاسب من جامعة المجمعة، وتدريب
  تعاوني مهندسَ برمجيات متدرّبًا في المركز الوطني للذكاء الاصطناعي (سدايا)
  بالرياض. الرقاقات من مهارات السيرة.
- **زر تحميل السيرة الذاتية** في الـhero باللغتين. الملف
  `public/Azzam-Alfahad-CV.pdf` يُخدَم بـ`application/pdf` مطابقًا للأصل بايت
  ببايت.
- **لا نص مؤقت في الموقع** — صفر تطابق لـ`Replace with…` في `src/`.

### أعطال أُصلحت (كلها رُصدت بالقياس لا بالنظر)
- **`a { color: inherit }` و`* { border-color }` كانتا خارج `@layer`.** CSS غير
  مُطبَّق في طبقة يتغلّب على كل طبقات Tailwind مهما كانت الـspecificity. الأثر:
  بريد التواصل لم يكن أزرق أبدًا، وروابط الشريط بلا hover، وحدّ
  `.btn-ghost:hover` بلون الـaccent لم يكن يُطبَّق. كلتاهما في `@layer base` الآن.
- **زر "الرئيسية" كان يعيد تحميل الصفحة** — الرابط الوحيد بلا hash. صار `/#top`.
- **روابط الشريط ترتدّ على الجوال** (بلاغ من صاحب المشروع على الموقع المنشور):
  من قسم الأعمال، الضغط على "الرئيسية" يذهب ثم يرجع إلى الأعمال. السبب اجتماع
  `scroll-behavior: smooth` مع `scroll-snap-type: y mandatory`: المتصفح يبقي
  القسم القديم هدفًا للالتقاط، وانطواء شريط المتصفح أثناء القفزة يعيد الالتقاط
  إليه. الحل سكربت في `Header.astro` يطفئ الالتقاط أثناء القفزة لروابط الصفحة
  نفسها ويعيده بعد 150ms من سكون التمرير. مُتحقَّق بالقياس (كل رابط يهبط على
  قسمه بالبكسل، والالتقاط يعود بلا حركة، وروابط صفحات دراسة الحالة تتنقّل
  عاديًا). **لم يُرَ على جوال حقيقي** — المتصفح المدمَّج لا يحرّك التمرير الناعم.
- **"نبذة" و"الأعمال" أطول من الشاشة على الجوال.** على 375×701 (أسوأ حالة
  والالتقاط شغّال): نبذة 900px والأعمال 755px. قرار صاحب المشروع: الفقرة الثانية
  في نبذة تختفي تحت 640px (`hidden sm:block`)، والأعمال تُضغط مسافاتها على الجوال
  فقط (`pb-6` للأقسام، `mb-5` للعناوين، وهوامش أصغر في `FeaturedProject`؛ `md:`
  يعيد قيم سطح المكتب). قيس بعدها على 375×701 و375×812 و390×844 و430×932
  و1280×800 باللغتين: لا قسم يفيض. أضيق هامش: الأعمال بالإنجليزية على 375×701
  = **6px**.
- **تتبّع المؤشر لم يكن يعمل إطلاقًا.** عنصر عند `z-index:-1` لا يستقبل حدث
  مؤشر لأن `<main>`/`<body>` يفوزان بفحص الإصابة. الاستماع نُقل إلى `window`.
- **تقطّع الحروف العربية** حيثما لمسها `uppercase`/`tracking-widest`/`.mono`.
- **الخط كان Arial** لا Geist في وقت سابق (سطر `font-family` في `globals.css`
  كان يُلغي الخط المحمَّل) — أُصلح.

---

## قيد العمل

**لا شيء.** كل العمل مدفوع إلى `master` (2026-09-10). الشجرة نظيفة.

**وين وقفنا بالضبط:** إصلاح الارتداد ومقاسات الجوال مُتحقَّق منهما بالقياس فقط،
وينتظران تجربة صاحب المشروع على جواله (البند 1 في "التالي").

---

## التالي

### 1. جرّب على جوال حقيقي
على الرابط العام:
- من قسم الأعمال اضغط "الرئيسية" ثم "تواصل" — لازم يصل ويبقى، بلا ارتداد.
- كل قسم يملأ الشاشة ولا يفيض، والالتقاط لا يقاوم التمرير في نبذة.
إن بقي الارتداد، فالسكربت في `Header.astro` هو المكان — جرّب رفع مهلة
`waitForQuiet` قبل أي شيء آخر.

### 2. أصلح فقدان موضع القسم عند تبديل اللغة
`src/components/LanguageToggle.astro`. المشكلة: `Base.astro` يبني المسار المقابل
من `Astro.url.pathname` وحده، والـhash لا يصل إلى الخادم أصلًا، فالانتقال من
`/#contact` يهبط على `/ar/` من أعلاها.

الحل: سكربت `is:inline` صغير يلحق `location.hash` بالـ`href` عند النقر:
```js
link.addEventListener("click", () => { link.href = link.href.split("#")[0] + location.hash; });
```
تحقّق بالانتقال من `/#contact` إلى العربية والوصول إلى `#contact`.

### 3. مشروع ثانٍ (اختياري، متى وُجد)
أضف ملفَّي MDX في `src/content/projects/{en,ar}/`. البنية جاهزة:
`FeaturedProject` يعرض الأول و`ProjectCard` يعرض البقية تلقائيًا — لا كود جديد.

---

## قرارات

كل قرار مع البديل المرفوض والسبب.

### الصفحة الواحدة والتخطيط
- **حذف الصفحات المفصولة، لا إبقاؤها كتحويلات.** طلب صريح من صاحب المشروع.
  الأثر المقبول: `/about` و`/contact` و`/work` ترد 404. لا شيء داخل الموقع كان
  يشير إليها.
- **صفحتا دراسة الحالة لم تُدمجا** — يُوصل إليهما بالضغط على البطاقة لا من
  الشريط، وهما جوهر نمط "Surface / Under the Hood".
- **`min-h-svh` لا `h-svh`، و`svh` لا `vh`.** الأولى لا تقص محتوى فاض؛ والثانية
  تتجنّب أن يدفع شريط متصفح الجوال أسفل القسم خارج المتناول.
- **`mandatory` لا `proximity`، مع مهرب.** الإجباري يعطي "شاشة = قسم" فعليًا،
  لكنه يقاوم المستخدم لو فاض قسم، فـ`@media (max-height: 700px)` يطفئه على
  الشاشات القصيرة — مُتحقَّق: على 667px يرجع `none`.
- **قاعدة الالتقاط في `<style is:global>` داخل صفحتَي الرئيسية، لا في
  `global.css`.** Astro يشحن أنماط الصفحة مع تلك الصفحة وحدها، فصفحتا دراسة
  الحالة تبقيان بتمرير عادي — مُتحقَّق: `scrollSnapType` عليهما `none`.
- **الشريط `sticky`** (كان `relative`): شاشة كاملة لكل قسم تُخفي التنقّل بعد أول
  تمرير. وخلفيته المعتمة ليست تجميلًا — الحقل مثبّت ويمتد خلفه.
- **التوسيط بـ`left-1/2` الفيزيائية لا `start-1/2` المنطقية.** التوسيط متماثل،
  فالمنطقية كانت ستقلبه بعيدًا عن المركز في RTL.

### خلفية Dot Lens
- **قبول جزيرة React ثانية** خلافًا لاتفاقية "الجزيرة الوحيدة". البديل المرفوض:
  CSS خالص — عدسة تكبّر النقاط حول المؤشر تحتاج حسابًا لكل بكسل.
- **الكود الخارجي أُضيف بتعديلات محدودة لا إعادة كتابة:** تصحيح قوس إغلاق زائد
  كان يُفشل `astro check`، وحذف `"use client"`، وإضافة الحلقة الموقوفة والمسح
  الأولي وانسحاب العدسة.
- **الثيم يُحلّ في غلاف منفصل.** `parseColor` يفهم hex/rgb فقط ويرجع أسود على
  `var(--bg)`. الغلاف يراقب `MutationObserver` على `data-theme` **و**`matchMedia`
  معًا — واحدة منهما كانت تترك نصف الحالات بلوحة قديمة.
- **`gl.clear()` بلون الخلفية فور إنشاء السياق.** كانفس `alpha:false` يبدأ أسود
  معتمًا حتى أول رسم. رُصد كلوح أسود فوق الصفحة، لا افتراضًا.
- **`IntersectionObserver` يوقف الحلقة خارج الشاشة** بدل تركها دائمة.
- **أحداث المؤشر من `window` لا من عنصر الحقل.** المحاولة الأولى
  (`pointer-events-none` على النص) كانت مكسورة: `elementFromPoint` فوق الـhero
  كان يرجع `MAIN` لا الحقل.
- **`fixed` لا `absolute`.** المطلقة تمدّ الكانفس إلى ارتفاع المستند كاملاً —
  أضعاف البكسلات في كل إطار لامتداد خارج الشاشة أصلًا.
- **توكن `--field-dot` منفصل عن `--text-muted`.** ربط الزخرفة بتوكن نص يعني أن
  كل تحسين لتباين القراءة يزيد ضجيج الخلفية — حصل فعليًا.

### النظام البصري
- **الأسهم محارف نصية لا SVG.** `→` يحمل `Bidi_Mirrored` فينقلب إلى `←`
  تلقائيًا في RTL. الـSVG أحدّ لكنه يحتاج قلبًا يدويًا — رُفض للمخاطرة.
- **تعبئة مسطّحة + لمعة داخلية، لا تدرّج.** التدرّج يعطي عمقًا أكثر لكنه يخاطر
  بمظهر قديم الطراز، وبساطة الموقع لا تحتمله.
- **لون المرور يغمق فاتحًا ويفتح داكنًا.** "التفتيح دائمًا" كان سيرقّق الأبيض
  على الأزرق ويخفض التباين؛ الاتجاهان الحاليان يرفعانه في الحالتين.
- **`.btn-ghost` طبقة ثالثة** لروابط المستودع/مباشر — كانت نصوصًا مسطّرة عارية
  بجوار زر ممتلئ، أي قفزة من لا تأكيد إلى أقصى تأكيد بلا وسط.

### المحتوى
- **دراسة الحالة أُعيدت كتابتها بتأطير هندسي لا بقصة الترحيل.** النسخة السابقة
  كانت تقود بخلفية PyQt5 وتضخّم بصمة الوجه في العنوان؛ الآن تقود بأن الصلاحيات
  مفروضة في قاعدة البيانات، وخلفية PyQt5 انتقلت إلى `architectureNotes` كسياق.
- **الـ`slug` بقي `secure-cloud-storage`** رغم تغيّر العنوان، حتى لا تنكسر
  الروابط.
- **رقم الجوال استُبعد من قسم التواصل، لكنه منشور داخل ملف السيرة.** استُبعد
  أولًا بقرار صريح بعد التنبيه إلى الزواحف، ثم رُفعت السيرة كما هي بقرار صريح
  لاحق. **لا تضف الرقم إلى قسم التواصل** بلا طلب جديد.

### تأسيسية (لا تزال سارية)
- `generateId` مخصص في `src/content/config.ts`: تصادم `slug` بين en/ar كان
  يُلغي إحدى النسختين من الـcollection بصمت.
- صفحات en/ar ملفات منفصلة متطابقة البنية، لا `[locale]` ديناميكي.
- `/deep` صفحة مستقلة قابلة للمشاركة والفهرسة، لا حالة UI مخفية.
- **التحقق قبل أي push بـ`npm run build` حصرًا**، لا `astro build` وحده — الأخير
  لا يشغّل `astro check`، وهذا بالضبط ما مرّر خطأ `Locale` محليًا وأفشل النشر.
- النشر عبر مشروع Vercel مربوط بـGit، لا رفع ملفات مباشر.
- الدومين المخصص مؤجل — الموقع على دومين Vercel الافتراضي
  (`portfolio-iota-ten-2fmes3t5ce.vercel.app`).

---

## مشاكل معروفة

1. **تبديل اللغة يفقد موضع القسم.** تفاصيل الإصلاح في "التالي" البند 2.
2. **شريط رمادي بنص متراكب أسفل الصفحة — في كروم صاحب المشروع وحده.** ليس من
   الموقع: على الرابط العام لا عنصر أسفل الشاشة سوى قسم الـhero وحقل النقاط، ولا
   فيض أفقي، وبريف (نفس محرك Chromium) يعرض الصفحة سليمة. المرجّح شريط أدوات
   Vercel (يظهر لمن سجّل دخوله في Vercel) أو إضافة أو تكبير محفوظ للموقع. لا تغيّر
   `DotLens.tsx` بسببه ما لم يظهر في نافذة كروم متخفية.
3. **لا اختبارات آلية إطلاقًا** — لا مشغّل ولا ملفات اختبار. كل تحقق حتى الآن
   يدوي في المتصفح، وهو نوع التغطية الذي لا يصمد أمام إعادة هيكلة.
4. **طبقة محتوى Astro تخزّن مؤقتًا.** أي تعديل على ملف `.mdx` قد لا يظهر في
   `npm run dev`. الحل: `rm -rf .astro` وإعادة تشغيل الخادم. لدغت مرتين.
5. **ثلاثة سلوكيات لم تُشاهَد متحركة:** الانجراف التلقائي للحقل، والتمرير
   الناعم، وانتقال الالتقاط بين الأقسام. المتصفح المدمَّج المستخدَم في الفحص
   يخنق `requestAnimationFrame`. المُتحقَّق منه بالقياس أن المنطق سليم (المراسي
   تهبط في مواضعها، والالتقاط يجذب أي موضع إلى حدود قسم، والعدسة تتبع المؤشر
   وتنسحب عن الروابط). **جرّبها في متصفح حقيقي.**
6. **لم يُختبر على جهاز لمس حقيقي.** بلا مؤشر، المتوقّع أن يعمل حقل النقاط
   بانجراف تلقائي فقط.
