# Progress

### تم

- سقالة كاملة: Astro 5.18 + Tailwind v4.3 + TypeScript strict + React island
  واحد (`LayerReveal`).
- ثنائي اللغة En (بدون بادئة) / Ar (`/ar`) مع RTL كامل، مختبر يدويًا بالمتصفح
  (تنقّل، اتجاه، أسهم، تبديل لغة).
- وضع فاتح/داكن كامل: توكنز CSS + زر تبديل + احترام `prefers-color-scheme` +
  حفظ الاختيار اليدوي، مختبر بالحالتين بالمتصفح.
- مشروع واحد ببنية كاملة (`secure-cloud-storage`, en + ar) يملأ كل حقول
  الـ schema بالكامل من الناحية التقنية — محتوى الأقسام العميقة لسا نص
  مؤقت (تفاصيل في "قيد العمل").
- مستودع GitHub عام: **azzam-dev/portfolio**، متصل بمشروع Vercel عبر
  GitHub App (auto-deploy عند كل push لـ `master`).
- إصلاح خطأ بناء حقيقي كان يُفشل النشر على Vercel: `Locale` type غير
  مُصدَّر من `src/i18n/utils.ts` رغم استخدامه في `Base.astro`
  (commit `5c2d2b6`).
- **الموقع منشور ومباشر على Vercel.** تأكد نجاح النشر على commit
  `eeff315` أو أحدث.
- توثيق المشروع: `CLAUDE.md` + `PROGRESS.md` في الجذر.
- **جولة واجهة على الصفحة الرئيسية والهيدر (2026-09-01):**
  - الـhero صار الاسم الكامل **Azzam Alfahad / عزام الفهد** بـ`text-5xl md:text-7xl`
    (مفتاح `home.name` جديد)، وحُذف سطر الـ`mono` الصغير الذي كان يحمل الاسم
    المختصر. الـtagline نزلت إلى `text-lg md:text-xl` والـsubline إلى
    `text-sm md:text-base` — الاسم صار العنوان بدل الشعار.
  - `home.workHeading` و`work.heading`: "Things I Built" → **"My Projects"**،
    و"أشياء بنيتها" → **"مشاريعي"**.
  - الهيدر: حُذف رابط الاسم `Azzam` يسارًا، وأُضيف **Home / الرئيسية**
    (مفتاح `nav.home`). الروابط الأربعة مركزية تمامًا من `md` فما فوق
    (`absolute left-1/2 -translate-x-1/2`) — قيست فعليًا: انحراف 0px عن مركز
    الحاوية. تحت `md` ترجع للتدفق الطبيعي عند حافة البداية.
  - **خلفية Dot Lens** (شيدر WebGL، كود خارجي من Originkit) تغطّي **الصفحة
    الرئيسية كاملة** باللغتين، عبر `DotLens.tsx` + غلاف `DotLensBackground.tsx`.
    مثبّتة على إطار العرض (`fixed inset-0`) فالمحتوى ينزلق فوقها والكانفس يبقى
    بحجم شاشة واحدة. **الرئيسية فقط** — تُحقّق منه بجلب كل الصفحات التسع
    وفحص ناتجها. و`Header.astro` أخذ `relative z-10` ليبقى فوق الحقل.

- **تحويل الموقع إلى صفحة واحدة (2026-09-01):** الأعمال والنبذة والتواصل صارت
  أقسامًا (`#work`, `#about`, `#contact`) داخل `index.astro` باللغتين، وروابط
  الشريط صارت مراسي إليها. **حُذفت ست صفحات** (`about`, `contact`, `work/index`
  بنسختيهما) ومعها مفتاح `work.heading` الذي لم يعد يُستعمل — من 12 صفحة مبنية
  إلى 6. صفحتا دراسة الحالة `/work/[slug]` و`/work/[slug]/deep` باقيتان كما هي
  (يُوصل إليهما بالضغط على البطاقة، لا من الشريط). أُضيف `scroll-behavior: smooth`
  على `html` — والبلوك الموجود أصلًا في آخر `global.css` يعيده إلى `auto` تحت
  `prefers-reduced-motion`.
- **إصلاح تقطّع الحروف العربية في عناوين الأقسام:** كان `mono uppercase
  tracking-widest` يعرض "مشاريعي" كـ"مـشا ريـعي". عناوين `ar/index.astro`
  الثلاثة تستخدم الآن `text-xs font-medium` بلا تباعد حروف ولا خط أحادي.

- **جولة وضوح وتباين وحجم (2026-09-01):** كل الأرقام أدناه مقيسة في المتصفح
  بحساب نسبة التباين، لا تقديرًا.
  - **التوكنز:** `--text-muted` من `#6B6E73` (**4.90** — بالكاد يمرّ AA وهو لون
    معظم نصوص الموقع) إلى `#52555A` (**7.17**، AAA)؛ وفي الداكن من `#8A8F98`
    (6.02) إلى `#A7ACB5` (**8.58**). `--accent` إلى `#1B47C4` (**7.32**) و
    `#7BA5FF` (**8.08**). `--border` قُوِّي (1.22→**1.50** فاتح، 1.27→**1.74**
    داكن) لأن `--surface` يبعد 1.04 فقط عن `--bg` فالحد وحده يفصل البطاقة.
  - **توكن جديد `--field-dot`** لحقل النقاط، منفصل عن `--text-muted`. كان الحقل
    يقرأ لون النص، فتغميق النص للتباين جعل الخلفية تزاحم القراءة فعليًا.
  - **الأحجام:** الوصف 14→18px، نص النبذة 16→18px، روابط التنقّل 12→16px،
    عنوان البطاقة 18→20px ومتنها 16px، البريد 18→20px.
  - **عناوين الأقسام صارت عناوين حقيقية** (`text-2xl md:text-3xl` بلون `--text`)
    بدل لصيقات صغيرة 12px بلون باهت — من 12px بتباين 4.90 إلى 30px بتباين 17.0.
- **إصلاح `a { color: inherit }` غير المُطبَّقة في طبقة:** CSS خارج `@layer`
  يتغلّب على كل أدوات Tailwind، فكانت تُلغي `text-[var(--accent)]` و
  `hover:text-[…]` على **كل** رابط. الأثر الفعلي: بريد التواصل كان يُعرض بلون
  النص العادي لا الأزرق، وروابط الشريط بلا أي تغيّر لون عند المرور. نُقلت إلى
  `@layer base`. مُتحقَّق منه بتجربة حاسمة: `<a class="text-[var(--accent)]">`
  مجرّد كان يعطي `rgb(232,233,235)` وصار `rgb(123,165,255)`.

- **شاشة واحدة لكل قسم (2026-09-01):** الأقسام الأربعة صارت `min-h-svh` بمحتوى
  موسَّط رأسيًا، مع `scroll-snap-type: y mandatory` على `html` و`snap-start` +
  `snap-always` على كل قسم — تمريرة واحدة = قسم واحد. قيس فعليًا: الأقسام
  الأربعة 800px بالضبط على شاشة 800px و812px على 812px، بلا فيض رأسي ولا أفقي.
  **والشريط العلوي صار `sticky`** بخلفية معتمة، لأن التنقّل كان سيختفي بعد أول
  تمرير ويصبح غير قابل للوصول.

- **ضبط الخطوط + إصلاح زر الرئيسية (2026-09-01):**
  - **`الرئيسية` كان يعيد تحميل الصفحة** بدل التمرير إليها: كان الرابط الوحيد بلا
    hash (`/` مجرّدة)، فالمتصفح يعامله كتنقّل كامل. أخذ القسم الأول `id="top"`
    وصار الرابط `/#top`. مُتحقَّق منه: من قسم التواصل (2465) الضغط ينقل إلى 65
    وعلامة في `window` تنجو — أي لا إعادة تحميل.
  - **خط عرض جديد للعناوين: Space Grotesk** (الاسم 700، عناوين الأقسام 600).
    المتن يبقى Inter والبريد JetBrains Mono. التبديل من متغيّر واحد
    `--font-display` في `global.css`.
  - **مقياس سائل بـ`clamp()`** بدل قفزتَي breakpoint: `.t-display` و`.t-h2` و
    `.t-lead` و`.t-body` في `@layer components`. قيس على 1265px: الاسم 84px،
    العناوين 40px، المتن 18.4px؛ وعلى 375px: 44px / 28px / 16px بلا فيض.
  - **العربية تتجاوز الخط اللاتيني والـtracking السالب** عبر `:lang(ar)`:
    IBM Plex Sans Arabic بـ`letter-spacing: normal` وارتفاع سطر أوسع. مُتحقَّق:
    الاسم العربي 76px بـ`ls: normal`.
  - قُلِّمت أوزان الخطوط المحمَّلة إلى المستعمَل فعليًا (Inter 400/500/600،
    عربي 400/500/600) بعد فحص الأصناف — لا `font-bold` في المشروع.

- **قسم التواصل صار قائمة مُسمّاة (2026-09-01):** `<dl>` دلالية بصفَّين —
  **البريد** `azzamalfahad23@gmail.com` و**لينكدإن**
  `linkedin.com/in/azzam-alfahad` (يُعرض بلا `www.`، ويشير إلى
  `https://www.linkedin.com/in/azzam-alfahad` بـ`target="_blank"` و
  `rel="noopener noreferrer"`). مفتاحان جديدان `contact.email` و
  `contact.linkedin` في اللغتين. القيم في العربية تحمل `dir="ltr"` وإلا
  عرضها المتصفح معكوسة داخل سياق RTL.
  **رقم الجوال طُلب ثم استُبعد بقرار صاحب المشروع** بعد التنبيه إلى أن نشره
  على موقع عام يعرّضه للزواحف — لا تُعِد إضافته بلا طلب صريح.

- **إعادة عرض الأقسام الثلاثة + توحيد صفحات دراسة الحالة (2026-09-01):**
  - **إطار مشترك:** سطر تمهيدي `01 / Work` بصنف `.eyebrow` قبل كل عنوان، فصارت
    الشاشات الأربع فصولًا في نظام واحد بدل أربع صفحات متجاورة.
  - **Work صار عرض "مشروع مميّز"** (`FeaturedProject.astro`) بدل بطاقة يتيمة في
    شبكة عمودين نصفها فارغ. يستهلك بيانات كانت مهدورة تمامًا في الـschema:
    `year` و`status` و`stack` و`surface.what`. البطاقات القديمة باقية لبقية
    المشاريع (`projects.slice(1)`) فالتخطيط لا يفترض مشروعًا واحدًا.
  - **About صار متدرّجًا:** جملة قيادية بلون `--text`، ثم فقرة مساندة رمادية،
    ثم شريط رقاقات "ما أشتغل به". **النص المؤقت المنشور استُبدل بنص حقيقي**
    مبني على ما تُظهره مشاريع هذا المستودع فعلًا (حدود المصادقة والتشفير،
    وتوثيق القرارات المرفوضة) — راجعه وصححه، فهو استنتاج لا إملاء.
  - **Contact صار صفوفًا محدودة بإطار** (`.link-row`) بحالة hover تلوّن الحد
    بـ`--accent`، بدل رابطين عاريين.
  - **صفحات دراسة الحالة الأربع ضُمّت للنظام:** `.eyebrow` و`.t-h2` و
    `.btn-accent` بدل أنماطها القديمة. هذا أصلح تقطّع الحروف العربية في 9
    مواضع كانت لا تزال على `mono uppercase tracking-widest`.
  - **مكدّس `--font-mono` صار يتضمّن "IBM Plex Sans Arabic"** كاحتياطي، لأن
    JetBrains Mono بلا محارف عربية والاحتياطي العام كان يفصل الحروف. وأُزيل
    `mono` كليًا عن النثر العربي في صفحات المشروع — بقي فقط على رقاقات
    التقنيات اللاتينية.
  - **قياس بعد كل هذا:** أقسام الرئيسية الأربعة 800px على شاشة 800 و812 على
    812 — بلا فيض. قسم About كان فاض 4px فقُلِّمت بطانة الجوال وجملة وحُذفت
    رقاقتان (656px → 546px)، ورُفع مهرب الشاشات القصيرة من 620px إلى 700px.

- **دراسة الحالة أُعيدت كتابتها من الواقع (2026-09-01):** كانت تصف **مشروعًا
  غير موجود** — تشفيرًا من جهة العميل عبر Web Crypto API وتخزينًا على AWS S3
  و«حتى مشغّل الخادم لا يقدر يقرأ». لا شيء من ذلك في أي مستودع: `gh repo list`
  يُظهر مستودعين فقط، والمشروع الحقيقي `azzam-dev/graduationproj-web` هو
  Next.js + Supabase بدخول اختياري بالوجه، بلا أي تشفير من جهة العميل.
  و`links: {}` كانت فارغة، فلم يكن لأحد سبيل للتحقق.
  الآن كل حقل مبني على مصدر مقروء: الطبقات الثماني والقرارات الأربعة والتحديان
  والأمان من `supabase/functions/face-login/index.ts` و`src/lib/face/models.ts`
  و`src/components/face-camera.tsx` و`PROGRESS.md` الخاص بذلك المشروع.
  العنوان صار "Cloud File Storage with Face Login" / "تخزين ملفات سحابي بدخول
  بالوجه"، والـ`slug` بقي `secure-cloud-storage` حتى لا تنكسر الروابط.
  `links.repo` و`links.live` مضبوطان ومُتحقَّق أنهما يردّان 200.
  **لم يبقَ أي نص "Replace with…" في الموقع.**

- **صقل دراسة الحالة، تأطير هندسي بدل قصة الترحيل (2026-09-01):** النسخة
  السابقة كانت تقود بخلفية PyQt5، وتضخّم بصمة الوجه في العنوان و"الفكرة"
  و"لمن هذا المشروع"، وتدفن أقوى ما في المشروع. الآن:
  - العنوان رجع **"Secure Cloud Storage" / "تخزين سحابي آمن"** بلا ذكر بصمة
    الوجه، فيطابق الـ`slug` أيضًا.
  - **الصلاحيات في قاعدة البيانات تتصدّر:** الجملة المفتاحية صارت "ملفات كل
    المستخدمين في قاعدة واحدة، يفصلها فلتر واحد قد يُنسى"، و"لماذا تهم" تشرح
    لماذا يفشل التخزين متعدّد المستخدمين في اتجاه واحد.
  - **بصمة الوجه نزلت** إلى جملة واحدة في "الفكرة" وقرارين تقنيين — مكانها
    الصحيح. وخلفية PyQt5 انتقلت إلى `architectureNotes` كسياق لا كعنوان.
  - **استُبدل تحدي بريف بثغرة صلاحيات حقيقية:** الوسيط كان يطابق المسارات
    العامة كبادئة، فورث `/register/face-setup` — وهي صفحة تكتب بيانات — صفة
    "عام" وفُتحت بلا جلسة. مصدرها `src/lib/supabase/session.ts` وتعليقاتها.
    أقوى للقارئ من عطل كاميرا في متصفح واحد، وأزال في الوقت نفسه تحفّظ
    "غير مُتحقَّق منه" الذي كان يرافق تحدي بريف.
  - أُضيف إلى قسم الأمان أن الوسيط يستعمل `getUser()` لا `getSession()`،
    وجملة صريحة أن هذا **تحكّم في الوصول لا تشفير طرفًا لطرف** — حتى لا يوحي
    العنوان بما لا يفعله التصميم.

- **العدسة تنسحب عن العناصر التفاعلية + زر السيرة الذاتية (2026-09-01):**
  - **العطل:** أكبر نقاط العدسة وأشدّها سطوعًا تقع تحت المؤشر بالضبط — وهو
    بالضبط موضع نص الرابط الذي أنت على وشك الضغط عليه، فتزاحمه.
  - **الإصلاح، طبقتان:** `onMove` في `DotLens` يفحص `event.target.closest()`
    على العناصر التفاعلية ويُفلت الهدف، فتعود العدسة إلى انجرافها التلقائي
    بدل أن تُقطع فجأة. استُعمل `event.target` لا `elementFromPoint` لأن
    المستمع على `window` أصلًا، فالعنصر الأعلى في اليد بلا فحص إصابة إضافي مع
    كل حركة. وثانيًا: `.link-row` أخذ خلفية `--surface` معتمة، فحتى الانجراف
    التلقائي لا يشفّ خلف النص.
  - **مُتحقَّق منه بمؤشر حقيقي:** مؤشر على فراغ ⇒ العدسة تتبعه؛ مؤشر على رابط
    LinkedIn عند (312,233) ⇒ العدسة بقيت عند (510,230) ولم تتبعه، وحالة hover
    على الصف مفعّلة أي المؤشر فوقه فعلًا.
  - **زر تحميل السيرة الذاتية** أسفل نص الـhero في اللغتين، بـ`.btn-accent`
    و`download` و`type="application/pdf"`. الملف في `public/Azzam-Alfahad-CV.pdf`
    ويُخدَم بـ`Content-Type: application/pdf` مطابقًا للأصل بايت ببايت
    (نفس الـmd5). ارتفاع الـhero صعد 187→273px، والأقسام باقية 812px.
  - **الملف يحوي رقم الجوال `+966 541231363`** — وهو الرقم الذي استُبعد سابقًا
    من قسم التواصل بقرار صريح. نشر السيرة ينشره فعليًا. **لم يُرفع بعد**؛
    القرار معلّق عند صاحب المشروع.

- **About أُعيد كتابته من السيرة الذاتية + صقل الزر (2026-09-01):**
  - **About صار وقائع لا استنتاجًا:** خرّيج علوم حاسب من **جامعة المجمعة**،
    وتدريب تعاوني مهندسَ برمجيات متدرّبًا في **المركز الوطني للذكاء الاصطناعي
    (سدايا)** بالرياض حيث بنى أداة داخلية لسير عمل الفريق. والفقرة الثانية تربط
    خزنة الملفات المكتبية (Python/PyQt مع تعرّف على الوجه) بإعادة الكتابة للويب.
    كل ذلك من `AzzamALfahad-CV.pdf` لا من تخمين.
  - **رقاقات "ما أشتغل به"** استُبدلت بمهارات السيرة الفعلية: Python، C++،
    TypeScript، SQL، PyQt، OpenCV، Next.js، PostgreSQL — بدل التخمين السابق.
  - **`.btn-accent` أُعيد تصميمه:** حواف 12px، وزن 600، ظل ملوّن بالـaccent
    يشتدّ عند المرور، ورفعة `translateY(-2px)` بدل الانزلاق الجانبي (الانزلاق
    كان يعاند سهم التحميل الذي يشير للأسفل)، وحالة ضغط ترجع الزر مكانه.
    والسهم يتحرك باتجاه معناه: أفقيًا لروابط الانتقال، ولأسفل لزر التحميل
    عبر `.btn-download`. توكنان جديدان `--shadow-accent` و
    `--shadow-accent-strong` لكل ثيم.
  - **أُضيفت حلقة تركيز للوحة المفاتيح** — لم يكن في المشروع أي `:focus-visible`
    إطلاقًا، والحلقة الافتراضية تكاد تختفي على زر ممتلئ بلون. وعلى الزر الملوّن
    تنقلب الحلقة إلى لون الخلفية لأن accent-على-accent غير مرئي.

### قيد العمل

لا شيء نصف مكتوب. محتوى دراسة الحالة الوحيدة اكتمل بمحتوى حقيقي في اللغتين
(انظر "تم")، ولم يبقَ في الموقع أي نص مؤقت.

البند التالي الطبيعي: مشروع ثانٍ في `src/content/projects/{en,ar}/` — البنية
كلها جاهزة له، و`FeaturedProject` يعرض الأول و`ProjectCard` يعرض البقية.

### قرارات

**قرارات الصفحة الواحدة (مع البديل المرفوض والسبب):**

- **حذف الصفحات المفصولة بدل إبقائها كنسخ رقيقة تعيد التوجيه.** طلب صاحب
  المشروع الصريح ألّا تبقى صفحات منفصلة. الأثر: `/about` و`/contact` و`/work`
  ترجع 404 الآن (تُحقّق منه). لا شيء داخل الموقع كان يشير إليها — صفحات دراسة
  الحالة تربط بين نفسها فقط، والبطاقات تربط بـ`/work/{slug}` مباشرة.
- **صفحتا دراسة الحالة لم تُدمجا.** هما جوهر نمط "Surface / Under the Hood"
  الموثَّق، ويُوصل إليهما بالضغط على البطاقة لا من الشريط، فلم يشملهما الطلب.
- **كل قسم بارتفاع شاشة كاملة مع التقاط إجباري.** يُلغي إصلاح `min-h-[80vh]`
  السابق على قسم التواصل: بما أن كل قسم صار `min-h-svh`، الصفحة طويلة كفاية
  ليصل كل قسم إلى أعلى الشاشة تمامًا (قيس: كل قسم يهبط عند 0px).
- **`min-h-svh` لا `h-svh`، و`svh` لا `vh`.** الأول لا يقص محتوى فاض عن الشاشة؛
  والثاني يتجنّب أن يدفع شريط متصفح الجوال أسفل القسم خارج المتناول.
- **`mandatory` لا `proximity`، مع مهرب.** الإجباري يعطي "شاشة = قسم" فعليًا،
  لكنه يقاوم المستخدم لو فاض قسم عن الشاشة، فـ`@media (max-height: 620px)`
  يطفئه على الشاشات القصيرة — مُتحقَّق منه: على شاشة 560px يرجع `none`.
- **قاعدة الالتقاط في `<style is:global>` داخل صفحتَي الرئيسية، لا في
  `global.css`.** Astro يشحن أنماط الصفحة مع تلك الصفحة وحدها، فصفحتا دراسة
  الحالة تبقيان بتمرير عادي — مُتحقَّق منه: `scrollSnapType` عليهما `none`.
- **الشريط العلوي صار `sticky` بخلفية معتمة** (كان القرار السابق إبقاءه
  `relative`). انقلب لأن شاشة كاملة لكل قسم تُخفي التنقّل بعد أول تمرير فيصير
  غير قابل للوصول. الخلفية المعتمة ليست تجميلًا: حقل النقاط مثبّت على إطار
  العرض ويمتد خلف الشريط، فبدونها تمرّ النقاط ونص الأقسام خلف الروابط.

**قرارات خلفية Dot Lens (مع البديل المرفوض والسبب):**

- **قبول جزيرة React ثانية، خلافًا لاتفاقية "الجزيرة الوحيدة".** البديل
  المرفوض: تأثير CSS خالص. عدسة تكبّر النقاط حول المؤشر تحتاج حسابًا لكل
  بكسل — لا تُبنى بـkeyframes. المقابل: `requestAnimationFrame` دائم، وهذا
  ما استدعى قرار الـ`IntersectionObserver` أدناه.
- **الكود الخارجي أُضيف بثلاثة تعديلات فقط، لا إعادة كتابة.** صُحِّح قوس
  إغلاق زائد (`})` بدل `}`) كان يُفشل `astro check`؛ وحُذف `"use client"`
  (توجيه Next.js بلا معنى في Astro)؛ وأُضيفت الحلقة الموقوفة والمسح الأولي.
  ما عدا ذلك الشيدر والمنطق كما وصلا حرفيًا.
- **الثيم يُحلّ في غلاف منفصل (`DotLensBackground`) لا داخل الشيدر.**
  `parseColor` يفهم hex/rgb فقط ويرجع أسود على `var(--bg)`. الغلاف يقرأ
  التوكنز بـ`getComputedStyle` ويراقب مصدرَي التغيير معًا: `MutationObserver`
  على `data-theme` (زر التبديل) و`matchMedia` (تفضيل النظام). مراقبة واحدة
  منهما كانت تترك نصف الحالات بلوحة قديمة.
- **`gl.clear()` بلون الخلفية فور إنشاء السياق.** كانفس `alpha:false` يبدأ
  أسود معتمًا حتى أول رسم. رُصد فعليًا كلوح أسود فوق الصفحة، لا افتراضًا.
- **`IntersectionObserver` يوقف حلقة الرسم خارج الشاشة**، بدل تركها دائمة.
  شيدر ملء الشاشة بحلقة 9 خلايا لكل بكسل يحرق البطارية بلا مقابل بصري وهو
  غير مرئي أصلًا.
- **أحداث المؤشر تُلتقط من `window` لا من عنصر الحقل نفسه.** المحاولة الأولى
  كانت `pointer-events-none` على النص فوق الكانفس، وكانت **مكسورة**: عنصر عند
  `z-index:-1` لا يستقبل حدث مؤشر أصلاً لأن `<main>` و`<body>` يغطّيان نفس
  المساحة ويفوزان بفحص الإصابة — والشفافية لا تُغيّر ذلك. `elementFromPoint`
  فوق الـhero كان يرجع `MAIN` لا الحقل. الاستماع على `window` مع فحص المستطيل
  يعطي نفس الحساب من مصدر تصل إليه الأحداث فعلاً، وأسقط الحاجة إلى أي عبث
  بـ`pointer-events` فبقيت الصفحة قابلة للنقر بشكل طبيعي.
- **الخلفية مثبّتة (`fixed`) لا مطلقة (`absolute`).** المطلقة كانت تمدّ الكانفس
  إلى ارتفاع المستند كاملاً — أضعاف البكسلات في كل إطار، لامتداد خارج الشاشة
  أصلاً. المثبّتة تبقى بحجم شاشة واحدة (قيس: 1265×720 بدل ارتفاع الصفحة).
- **التوسيط بـ`left-1/2` الفيزيائية لا `start-1/2` المنطقية.** التوسيط
  متماثل، فالخاصية المنطقية كانت ستقلبه بعيدًا عن المركز في RTL.

**قرارات سابقة (مع البديل المرفوض والسبب):**

- **النشر عبر مشروع Vercel مربوط بـGit (auto-deploy)، لا رفع ملفات مباشر.**
  جُرّب `deploy_to_vercel` (رفع ملفات بدون Git) أثناء التشخيص، ورُفض كحل
  دائم لأنه لا يعطي نشرًا تلقائيًا عند كل push — كان الهدف الفعلي.
- **لما فشل الربط الآلي GitHub↔Vercel عبر أدوات MCP بخطأ `403` متكرر على
  عدة مشاريع وأسماء مختلفة، أوقفنا إعادة المحاولة الآلية وانتقلنا لاستيراد
  يدوي من لوحة Vercel مباشرة (vercel.com/new).** الاستمرار بالمحاولات
  الآلية رُفض لأنه كان يولّد مشاريع Vercel يتيمة متكررة بدون حل السبب
  (قيد صلاحيات على التوكن نفسه، غير قابل للحل بإعادة المحاولة).
- **إصلاح خطأ `Locale` بإعادة تصديره من `src/i18n/utils.ts`
  (`export type { Locale }`)، لا بتغيير كل الملفات لتستورد من
  `src/i18n/ui.ts` مباشرة.** البديل الثاني رُفض لأنه يكسر الاتفاقية
  القائمة: كل صفحة/مكوّن يستورد أدوات اللغة من `i18n/utils`، لا من `ui`.
- **التحقق المحلي قبل أي push يكون بـ `npm run build` حصرًا، لا
  `npx astro build` وحده.** السبب: `astro build` وحده لا يشغّل
  `astro check`، وهذا بالضبط ما سمح لخطأ الـLocale يمر محليًا ويفشل على
  Vercel — حصل فعليًا، ليس افتراضًا نظريًا.

**قرارات تأسيسية سابقة (لا تزال سارية):**

- `generateId` مخصص في `src/content/config.ts` بدل افتراضي Astro: تصادم
  حقل `slug` بين en/ar (نفس القيمة في الملفين) كان يُلغي إحدى النسختين من
  الـ collection بصمت.
- صفحات en/ar ملفات منفصلة متطابقة البنية، لا `[locale]` ديناميكي — يبسّط
  منطق تبديل اللغة على حساب بعض التكرار.
- مسار `/deep` صفحة مستقلة قابلة للمشاركة والفهرسة، لا حالة UI مخفية.
- الدومين المخصص مؤجل — الموقع يستخدم دومين Vercel الافتراضي حاليًا.

### التالي

خطوة واحدة محددة، بهذا الترتيب بالضبط (كل بند يعتمد على اللي قبله):

1. اكتب `deep.security` في `en/secure-cloud-storage.mdx`: دالة اشتقاق
   المفتاح المستخدمة فعليًا، خوارزمية التشفير، وأين تُحفظ المفاتيح على
   جهاز المستخدم.
2. اكتب `deep.architectureNotes`: فقرة قصيرة تربط طبقات `deep.layers`
   الموجودة فعليًا في نفس الملف بقرار الأمان من الخطوة 1.
3. اكتب `deep.challenges[0]` بأجزائه الثلاثة: مشكلة تقنية واحدة حقيقية
   واجهتها أثناء بناء هذا المشروع تحديدًا.
4. اكتب `deep.whatIdImprove[0]`: قيد حقيقي واحد تعرفه في التصميم الحالي.
5. طبّق نفس المحتوى (مترجمًا) على `ar/secure-cloud-storage.mdx`.
6. شغّل `npm run build` للتحقق (لازم 0 errors)، ثم `git push`.

لا تبدأ بأي بند آخر (مشروع ثانٍ، صفحة about، دومين مخصص) قبل إكمال هذا.

### مشاكل معروفة

- **تبديل اللغة يفقد موضع القسم.** `Base.astro` يبني المسار المقابل من
  `Astro.url.pathname` وحده، والـhash لا يصل إلى الخادم أصلًا. فالانتقال من
  `/#contact` يهبط على `/ar/` من أعلاها. يحتاج سكربت عميل يلحق `location.hash`
  بالرابط. **لم يُطبَّق.**
- **التمرير الناعم لم يُشاهَد متحركًا.** الأنيميشن يحتاج حلقة الرسم التي يخنقها
  المتصفح المدمَّج. المُتحقَّق منه أن المرساة نفسها تعمل: بتعطيل الأنيميشن يهبط
  كل قسم في مكانه الصحيح (32/32/160 بكسل من أعلى الشاشة).
- **الانجراف التلقائي لحقل Dot Lens لم يُشاهَد متحركًا.** المتصفح المدمَّج
  المستخدَم في الفحص يخنق `requestAnimationFrame` فلا تتقدّم الحلقة إلا إطارات
  معدودة عند كل تفاعل. **تتبّع المؤشر مُتحقَّق منه فعليًا** بأحداث مؤشر حقيقية:
  العدسة لحقت المؤشر إلى (435,270) مقابل مؤشر عند (435,268) في الوضع الفاتح،
  وتتبّعت في الوضع الداكن كذلك. المُتحقَّق منه أيضًا: ترجمة الشيدر ورسمه
  (مدى بكسلات 34→489 = نقاط فعلية)، والكانفس 1265×720 ثابت عند التمرير حتى
  أسفل الصفحة، ومطابقة ألوان الغلاف للتوكنز في الوضعين، وبقاء البطاقات
  والروابط وأزرار الهيدر قابلة للنقر فوق الحقل. **ما لم يُختبر: السلوك على
  جهاز لمس حقيقي** (لا مؤشر، فالمتوقّع انجراف تلقائي فقط).
- لا يوجد test suite في المشروع حتى الآن.
- 4 مشاريع Vercel يتيمة من محاولات النشر الفاشلة، بلا أي كود مرتبط بها:
  `portfolio`, `azzam-portfolio`, `azzam-portfolio-site`,
  `azzam-dev-portfolio`. حذفها اختياري (Settings → Delete Project لكل
  واحد)، ولا تؤثر على المشروع المنشور فعليًا.
- صفحة `about.astro` (en + ar) لسا نص عام غير مخصص، ليست خطأ لكنها ناقصة.
