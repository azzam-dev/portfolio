# Progress

### تم

- سقالة كاملة: Astro 5 + Tailwind v4 + TypeScript strict + React island واحد.
- ثنائي اللغة En (افتراضي، بدون بادئة) / Ar (`/ar`) مع RTL كامل ومختبر يدويًا
  بالمتصفح (تنقّل، اتجاه، أسهم، تبديل لغة).
- وضع فاتح/داكن: توكنز CSS + زر تبديل + احترام `prefers-color-scheme`
  ومحافظة على الاختيار اليدوي عبر localStorage. مختبر بالمتصفح في الحالتين.
- مكوّن **LayerReveal** (الفكرة المحورية للموقع): يبني رسم الطبقات من
  `deep.layers` في المحتوى، مع stagger عند الظهور وaccordion لكل طبقة.
  يحترم `prefers-reduced-motion`، وله fallback نصي كامل داخل `<noscript>`.
- مشروع واحد كامل البنية: `secure-cloud-storage` (en + ar) يملأ كل حقول
  الـ schema — لكن حقول `deep.architectureNotes` / `challenges` /
  `security` / `whatIdImprove` لسا نص "Replace with..." وليست محتوى حقيقي.
- مستودع GitHub عام: **azzam-dev/portfolio** — تم إنشاؤه ورفع الكود عبره
  `gh` CLI بعد تثبيته وتسجيل الدخول من جلسة العمل.
- تطبيق Vercel-GitHub مثبّت على الحساب، والمستودع مربوط يدويًا من لوحة
  Vercel (المستخدم استورده بنفسه عبر vercel.com/new بعد فشل الربط الآلي).
- إصلاح خطأ بناء حقيقي فشّل أول نشر: `astro check` (يشغّله `npm run build`
  الذي يستخدمه Vercel) اكتشف إن `Locale` type غير مُصدَّر من
  `src/i18n/utils.ts` رغم استخدامه في `Base.astro`. تم الإصلاح + تنظيف
  تحذيرين (imports غير مستخدمة) بكومنت `5c2d2b6`.

### قيد العمل

- انتظار تأكيد إن نشر Vercel لكومنت `5c2d2b6` بنى بنجاح. آخر لوج شافه
  المستخدم كان يبني كومنت قديم (`2b15cf5`، قبل الإصلاح) — يحتمل يكون بناء
  كان أصلًا في الطابور من الرفعة الأولى، مو الرفعة المُصحَّحة.

### قرارات

- **`generateId` مخصص** في `src/content/config.ts` بدل افتراضي Astro: الافتراضي
  يستخدم حقل `slug` من الـ frontmatter كمعرّف، وبما إن en/ar لنفس المشروع
  يشتركان بنفس قيمة `slug`، كان يُلغي إحدى النسختين من الـ collection بصمت
  (صفحات المشروع كانت لا تُبنى إطلاقًا قبل هذا الاكتشاف).
- **صفحات en/ar ملفات منفصلة متطابقة البنية**، لا `[locale]` ديناميكي —
  يبسّط منطق زر تبديل اللغة (إضافة/حذف بادئة `/ar` فقط) على حساب بعض
  التكرار بين الملفات.
- **مسار `/deep` منفصل عن `/work/[slug]`** (مو حالة UI مخفية) — قابل
  للمشاركة، يُفهرس، ويشتغل بدون JS.
- تأجيل الدومين المخصص بطلب المستخدم — يستخدم دومين Vercel الافتراضي حاليًا.

### التالي

- تعبئة محتوى `secure-cloud-storage` الحقيقي بدل الـ placeholders (خصوصًا
  `decisions` و`whatIdImprove` — هذي أهم نقطة تميّز في فلسفة الموقع).
- إضافة المشروع الثاني الذي يعمل عليه المستخدم حاليًا (en + ar).
- تخصيص فقرات `about.astro` / `ar/about.astro` (لسا نص عام/placeholder).
- تنظيف مشاريع Vercel اليتيمة (انظر "مشاكل معروفة").
- ربط دومين مخصص، OG images لكل مشروع، sitemap، تحليلات.

### مشاكل معروفة

- **لا يوجد test suite** في المشروع حتى الآن.
- محاولات النشر الآلي عبر أدوات Vercel MCP فشلت باستمرار بخطأ
  `403: permission to create a Production Deployment`، حتى مع مشاريع جديدة
  تمامًا ومع target=preview — يبدو قيد فعلي على صلاحية النشر للتوكن
  المستخدم في هذه الجلسة، غير قابل للحل بإعادة المحاولة. تم الالتفاف عليه
  بالنشر اليدوي من لوحة Vercel.
- خلّفت المحاولات الفاشلة عدة **مشاريع Vercel فاضية/يتيمة** بنفس اسم أو
  أسماء قريبة من المستودع: `portfolio`, `azzam-portfolio`,
  `azzam-portfolio-site`, `azzam-dev-portfolio`. تحتاج حذف يدوي من
  Settings → Delete Project لكل واحد إذا أزعجت المستخدم — لا تؤثر على
  المشروع الفعلي المستخدَم.
- محليًا، `npx astro build` وحده **لا يشغّل** `astro check` — استخدم
  `npm run build` دائمًا قبل الدفع، وإلا ممكن يمر خطأ type محليًا ويفشل
  على Vercel (صار هذا فعليًا مرة واحدة).
